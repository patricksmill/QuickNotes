<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- when the view is selected (i.e. note.isPinned()==true) -->
    <item android:state_selected="true"
        android:drawable="@drawable/ic_pin_filled"/>
    <!-- default (unpinned) -->
    <item android:drawable="@drawable/ic_pin_outline"/>
</selector>